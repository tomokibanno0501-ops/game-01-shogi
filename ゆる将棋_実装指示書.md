# ゆる将棋 実装指示書（Claude Code向け）

## 1. プロジェクト概要

Flutterを用いた標準将棋アプリ「ゆる将棋」を実装する。人間 vs CPU（ランダムAI）の対局が可能な、初心者向けのシンプルな将棋アプリを目指す。

## 2. 対象OS

- 初期リリースはAndroidのみ
- ただし、将来的なiOS移植を見据え、Android専用APIへの依存は避けること

## 3. 技術スタック

- Flutter（Dart）
- 状態管理：シンプルなもの（Provider や setState ベースなど、過剰に複雑な構成にしない）
- 外部パッケージは必要最小限に留める

## 4. 機能要件

### 4.1 盤面・駒

- 9x9の将棋盤を表示
- 駒はアイコンまたは漢字の駒記号で表示（歩・香・桂・銀・金・角・飛・玉など）
- 初期配置は標準将棋のルールに従う
- 自分の駒・相手の駒が視覚的に区別できること（向きや色で表現）

### 4.2 駒の移動・操作

- 駒をタップすると、その駒が移動可能なマスをハイライト表示する（初心者向け支援）
- ハイライトされたマスをタップすると駒が移動する
- 成り（駒が敵陣に入った際の昇格）の選択ダイアログを表示する

### 4.3 ルール検出（反則手の完全検出）

以下を含む、標準将棋の反則手をすべて検出し、該当する手を指せないようにする。

- 二歩
- 行き所のない駒（歩・香が最奥段、桂が最奥2段に移動する手）
- 打ち歩詰め
- 王手放置（自玉に王手がかかったままの手）
- その他、通常の将棋ルールで禁止されている手

### 4.4 CPU対戦

- CPUはランダムに合法手を選択する（強さの調整は不要）
- CPUの手番になったら、少し間を置いてから指す（人間が見やすいように）

### 4.5 勝敗判定

- 詰みの検出
- 投了機能（人間側がいつでも投了できるボタン）

### 4.6 UI・操作性

- 画面向きは縦持ち固定
- 効果音は駒移動音のみ（最小限）
- タイマー・棋譜機能は実装しない（初期版の対象外）

## 5. ストア配信要件

- Google Play向けのアプリアイコン・スプラッシュ画面を用意する
- パッケージ名（applicationId）を決定する
- `flutter build appbundle` でGoogle Play提出用の.aabファイルを生成できる状態にする

### 参考：iOS対応時に必要になる追加作業（今回は対象外）

- Xcodeでのビルド設定
- Appleの署名・証明書関連の設定
- App Store Connect側の設定

## 6. 実装優先順位

1. 盤面・駒の初期配置表示
2. 駒の選択・移動先ハイライト表示
3. 基本的な駒の移動ロジック
4. 反則手検出（二歩・行き所のない駒・打ち歩詰め・王手放置など）
5. 成りの処理
6. CPU（ランダムAI）の実装
7. 詰み判定・投了機能
8. 効果音の追加
9. 画面向き固定・全体UI調整
10. Android実機での動作確認

## 7. 検査（QA）観点

- 全ての反則手パターンが正しく防止されているか
- 二歩・打ち歩詰めなど、見落としやすいルールが漏れなく実装されているか
- 縦持ち固定が正しく機能しているか（回転してもレイアウトが崩れないか）
- Android実機（複数機種・画面サイズがあれば理想）での表示崩れ・動作不良がないか
- CPU側が反則手を指さないか

## 8. 開発の進め方（Claude Codeへの依頼の仕方）

大きな機能を一度に依頼するのではなく、上記「6. 実装優先順位」に沿って、「まずは1と2（盤面表示とハイライト移動）だけ実装して」のように小さく区切って依頼すると、検査（レビュー）もしやすくなる。
